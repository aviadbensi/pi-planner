{
  "name": "pi-planner-collab-server",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency LAN collaboration server for PI Planner (HTTP + SSE).",
  "main": "pi-server.js",
  "scripts": {
    "start": "node pi-server.js",
    "test": "node --test"
  },
  "engines": { "node": ">=18" }
}
