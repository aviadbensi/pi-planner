# macOS
.DS_Store

# Server runtime data — live plans are local state, not source
server/plan.json
server/projects/

# Packaged build artifact (regenerate from pi-planner.html)
*.zip

# Node
node_modules/
